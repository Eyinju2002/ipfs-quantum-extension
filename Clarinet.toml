[project]
name = "ipfs-quantum-extension"
authors = ["Quantum Research Team"]
description = "IPFS-enabled quantum verification platform for smart contract certification"
telemetry = false
requirements = []
[contracts.quantum-verification]
path = "contracts/quantum-verification.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
